common/.config/tmux/current-theme.conf
common/.config/nvim/lazy-lock.json
common/.config/tmux/plugins/
.DS_store

# Electron app caches — contain auth tokens and credentials in LevelDB files
**/leveldb/
**/IndexedDB/
**/Cache/
**/GPUCache/
teams-for-linux/
outlook-for-linux/

# Private Claude instructions — not for public dotfiles
common/.claude/CLAUDE.local.md

# Machine-local shell state — installer paths, work-only exports
common/.zshrc.local

# Claude local permissions — contains absolute work paths
.claude/settings.local.json
